Volumes are the preferred mechanism for persisting data generated by and used by Docker containers

$ docker volume  --help //get information
$ docker volume create
$ docker volume ls
$ docker volume inspect
$ docker volume rm
$ docker volume prune

Instead of deleting containers one by one of docker ps -a , we can use docker container prune.  and for docker ps (running containers) we can use  docker rm $(ps -aq)

Use of Volumes
===========
- Decoupling container from storage
- Share volume (storage/data) among different containers
- Attach volume to container
- On deleting container volume does not delete

Commands
$ docker pull jenkins/jenkins
$ docker run --name MyJenkins1 -v myvol1:/var/jenkins_home -p 8080:8080 -p 50000:50000 jenkins/jenkins
$ docker run --name MyJenkins2 -v myvol1:/var/jenkins_home -p 9090:8080 -p 60000:50000 jenkins/jenkins
$ docker run --name MyJenkins3 -v /home/kaiserhamid/Documents/2.tigerit/09082021/docker/docker-volume/Jenkins_Home:/var/jenkins_home -p 9191:8080 -p 40000:50000 jenkins/jenkins
