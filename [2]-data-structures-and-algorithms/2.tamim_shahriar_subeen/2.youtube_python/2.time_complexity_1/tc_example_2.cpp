# include <stdio.h>

// Time Complexity of this program is = O(1)
int main()
{
    int n = 100, result;

    // scanf("%d", &n);
    // result = n * (n + 1) / 2;
    // printf("result = %d\n", result);

    result = n * (n + 1) / 2;
    printf("result = %d\n", result);

    return 0;
}
