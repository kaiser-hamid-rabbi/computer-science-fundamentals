/* 
=====================================================
Multifile Compilation (Main file)
=====================================================
 */

#include <iostream>
#include "math_utils.h"

int main()
{
    std::cout << pow(5, 3) << "\n";
    std::cout << pow(5) << "\n";
}